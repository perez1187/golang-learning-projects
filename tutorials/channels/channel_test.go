package main

import (
	"testing"
)

func TestAddUser(t *testing.T) {

	// server := NewServer()
	// server.Start()

	// for i := 0; i < 10; i++ {
	// 	go func(i int) {
	// 		server.userch <- fmt.Sprintf("user_%d", i)
	// 		// server.addUser(fmt.Sprintf("user_%d", i))
	// 	}(i)
	// }
}

// func TestAddUserTwo(t *testing.T) {
// 	serverTwo := NewServer()
// 	for i := 0; i < 10; i++ {
// 		go serverTwo.addUser(fmt.Sprintf("user_%d", i))
// 	}
// }
